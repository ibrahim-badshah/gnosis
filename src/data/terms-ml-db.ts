import type { Term } from './types';

export const mlTerms: Term[] = [
  { id: 'neural-network', word: 'Neural Network', category: 'Machine Learning', pronunciation: 'NOOR-ul NET-wurk',
    explanation: { beginner: 'A neural network is a computer system inspired by the human brain — layers of connected "neurons" that learn patterns from data, like recognizing cats in photos.', intermediate: 'Neural networks consist of layers of interconnected nodes. Input passes through hidden layers with learned weights and activation functions to produce output. Trained via backpropagation with gradient descent. Universal function approximators.', expert: 'Deep networks exploit hierarchical feature learning. Initialization (Xavier/He), normalization (BatchNorm, LayerNorm), and residual connections address vanishing gradients. Neural Architecture Search automates topology design. Lottery ticket hypothesis suggests sparse trainable subnetworks exist. Neural tangent kernels connect infinite-width networks to kernel methods.' },
    codeExample: {"language":"python","code":"import tensorflow as tf\nmodel = tf.keras.models.Sequential([\n  tf.keras.layers.Dense(128, activation=\"relu\", input_shape=(784,)),\n  tf.keras.layers.Dense(10, activation=\"softmax\")\n])\nmodel.compile(optimizer=\"adam\", loss=\"sparse_categorical_crossentropy\")","description":"Simple Neural Network in TensorFlow"},
    relatedTerms: ['deep-learning', 'backpropagation', 'gradient-descent', 'activation-function'],
    historicalContext: 'Perceptron introduced by Frank Rosenblatt (1958). Deep learning revolution began with AlexNet winning ImageNet in 2012.',
    tags: ['model', 'deep-learning', 'fundamental'] },
  { id: 'gradient-descent', word: 'Gradient Descent', category: 'Machine Learning', pronunciation: 'GRAY-dee-unt deh-SENT',
    explanation: { beginner: 'Gradient descent is like walking downhill in fog — you feel the slope beneath your feet and step in the steepest downward direction to reach the bottom (the best solution).', intermediate: 'Gradient descent minimizes a loss function by iteratively updating parameters in the direction of steepest descent. Learning rate controls step size. Variants: batch (full dataset), stochastic (SGD, one sample), mini-batch (subset).', expert: 'Adam optimizer combines momentum with adaptive learning rates (RMSprop). Learning rate scheduling (cosine annealing, warm restarts) improves convergence. Second-order methods (L-BFGS, natural gradient) use curvature information. Gradient clipping prevents exploding gradients. Gradient noise helps escape sharp minima (flat minima generalize better).' },
    codeExample: { language: 'python', code: '# Simple gradient descent\ndef gradient_descent(f_grad, x, lr=0.01, steps=100):\n    for _ in range(steps):\n        grad = f_grad(x)\n        x = x - lr * grad\n    return x', description: 'Basic gradient descent loop' },
    relatedTerms: ['neural-network', 'backpropagation', 'loss-function', 'optimizer'],
    historicalContext: 'The method of steepest descent was proposed by Cauchy in 1847. SGD adapted for ML by Robbins and Monro (1951).',
    tags: ['optimization', 'training', 'fundamental'] },
  { id: 'overfitting', word: 'Overfitting', category: 'Machine Learning', pronunciation: 'OH-ver-fit-ing',
    explanation: { beginner: 'Overfitting is when a model memorizes the training data instead of learning general patterns — like a student who memorizes answers but can\'t solve new problems.', intermediate: 'Overfitting occurs when a model performs well on training data but poorly on unseen data. Signs: large gap between training and validation accuracy. Mitigation: regularization (L1/L2), dropout, early stopping, data augmentation, cross-validation.', expert: 'The bias-variance tradeoff formalizes overfitting: high variance = overfitting, high bias = underfitting. Double descent challenges this — very large models can overfit then recover. Regularization acts as implicit Bayesian prior. Dropout approximates ensemble averaging. Minimum description length principle relates model complexity to generalization.' },
    relatedTerms: ['regularization', 'cross-validation', 'bias-variance', 'underfitting'],
    historicalContext: 'The concept of overfitting has been understood since the early days of statistical modeling in the 19th century.',
    tags: ['training', 'generalization', 'problem'] },
  { id: 'cnn', word: 'Convolutional Neural Network', category: 'Machine Learning', pronunciation: 'kon-voh-LOO-shun-ul NET-wurk',
    explanation: { beginner: 'A CNN is a neural network that scans images with small filters to detect patterns like edges, shapes, and objects — the technology behind photo recognition.', intermediate: 'CNNs use convolutional layers that slide learned filters over input to create feature maps. Pooling layers reduce spatial dimensions. Architecture: conv → activation → pooling → ... → fully connected. Excel at image tasks.', expert: 'Modern architectures: ResNet (skip connections), DenseNet (dense connections), EfficientNet (compound scaling). Depthwise separable convolutions reduce parameters (MobileNet). Attention mechanisms (SE-Net, CBAM) enhance feature selection. Vision Transformers (ViT) challenge CNN dominance. Dilated convolutions increase receptive field without pooling.' },
    codeExample: {"language":"python","code":"from tensorflow.keras.layers import Conv2D, MaxPooling2D, Flatten, Dense\nmodel = tf.keras.models.Sequential([\n  Conv2D(32, (3,3), activation=\"relu\", input_shape=(28,28,1)),\n  MaxPooling2D((2,2)),\n  Flatten(),\n  Dense(10, activation=\"softmax\")\n])","description":"Convolutional Neural Network structure"},
    relatedTerms: ['neural-network', 'deep-learning', 'image-recognition', 'pooling'],
    historicalContext: 'Inspired by Hubel and Wiesel\'s work on the visual cortex (1962). LeNet-5 by Yann LeCun (1998) was a landmark.',
    tags: ['architecture', 'vision', 'deep-learning'] },
  { id: 'rnn', word: 'Recurrent Neural Network', category: 'Machine Learning', pronunciation: 'ree-KUR-unt NET-wurk',
    explanation: { beginner: 'An RNN processes sequences (like sentences) by remembering what it has seen before — like reading a book and remembering earlier chapters to understand the current page.', intermediate: 'RNNs have recurrent connections creating internal memory. Hidden state is updated at each time step, capturing sequential dependencies. Suffer from vanishing/exploding gradients. LSTM and GRU architectures address this.', expert: 'LSTMs use forget/input/output gates to control information flow. GRUs simplify to reset/update gates. Bidirectional RNNs process sequences both ways. Attention mechanisms (Bahdanau) led to Transformers which largely replaced RNNs. State-space models (Mamba, S4) offer linear-time alternatives.' },
    codeExample: {"language":"python","code":"from tensorflow.keras.layers import SimpleRNN, Dense\nimport tensorflow as tf\nmodel = tf.keras.models.Sequential([\n  SimpleRNN(64, return_sequences=False, input_shape=(10, 32)),\n  Dense(1)\n])","description":"Recurrent Neural Network layer"},
    relatedTerms: ['lstm', 'transformer', 'sequence-model', 'nlp'],
    historicalContext: 'Developed in the 1980s. LSTM invented by Hochreiter and Schmidhuber in 1997.',
    tags: ['architecture', 'sequence', 'deep-learning'] },
  { id: 'random-forest', word: 'Random Forest', category: 'Machine Learning', pronunciation: 'RAN-dum FOR-est',
    explanation: { beginner: 'A random forest makes predictions by asking many decision trees for their opinion and going with the majority vote — like polling a crowd for the best answer.', intermediate: 'Random forests are ensembles of decision trees trained on random subsets of data (bagging) with random feature subsets. Reduces overfitting compared to individual trees. Provides feature importance measures. Works well with minimal tuning.', expert: 'Bagging reduces variance while maintaining low bias. Out-of-bag error provides cross-validation estimate. Feature importance via mean decrease in impurity or permutation importance. Extremely Randomized Trees (ExtraTrees) further randomize split points. Isolation Forest variant detects anomalies.' },
    codeExample: {"language":"python","code":"from sklearn.ensemble import RandomForestClassifier\n# X_train, y_train are your data\nclf = RandomForestClassifier(n_estimators=100, max_depth=5)\n# clf.fit(X_train, y_train)\n# predictions = clf.predict(X_test)","description":"Random Forest with Scikit-Learn"},
    relatedTerms: ['decision-tree', 'ensemble-learning', 'bagging', 'xgboost'],
    historicalContext: 'Introduced by Leo Breiman in 2001, building on his earlier work on bagging (1996).',
    tags: ['ensemble', 'classification', 'regression'] },
  { id: 'svm', word: 'Support Vector Machine', category: 'Machine Learning', pronunciation: 'suh-PORT VEK-tor muh-SHEEN',
    explanation: { beginner: 'SVM draws the best possible line (or surface) to separate different categories of data points — like drawing a fence between two groups with the widest possible gap.', intermediate: 'SVMs find the hyperplane that maximizes the margin between classes. Support vectors are the closest data points to the boundary. Kernel trick maps data to higher dimensions for non-linear separation. Effective in high-dimensional spaces.', expert: 'The dual formulation enables the kernel trick: K(x,y) = φ(x)·φ(y). Common kernels: RBF, polynomial, sigmoid. ν-SVM parameterizes the fraction of support vectors. SMO (Sequential Minimal Optimization) efficiently solves the QP. One-class SVM for anomaly detection. SVMs are maximum margin classifiers related to structural risk minimization.' },
    codeExample: {"language":"python","code":"from sklearn.svm import SVC\nclf = SVC(kernel=\"rbf\", C=1.0)\n# clf.fit(X_train, y_train)\n# predictions = clf.predict(X_test)","description":"Support Vector Machine Classifier"},
    relatedTerms: ['classification', 'kernel', 'hyperplane', 'regularization'],
    historicalContext: 'Developed by Vladimir Vapnik and colleagues at AT&T Bell Labs in the 1990s.',
    tags: ['classification', 'margin', 'kernel'] },
  { id: 'backpropagation', word: 'Backpropagation', category: 'Machine Learning', pronunciation: 'BAK-prop-uh-GAY-shun',
    explanation: { beginner: 'Backpropagation is how neural networks learn from mistakes — it figures out how much each connection contributed to the error and adjusts them accordingly.', intermediate: 'Backpropagation computes gradients of the loss function with respect to each weight by applying the chain rule backward through the network. These gradients guide weight updates via gradient descent.', expert: 'Backprop is reverse-mode automatic differentiation applied to computational graphs. Complexity equals forward pass (one backward pass). Gradient checkpointing trades compute for memory. Alternatives: perturbation-based methods, forward-mode AD for few parameters, synthetic gradients for asynchronous training.' },
    relatedTerms: ['neural-network', 'gradient-descent', 'chain-rule', 'loss-function'],
    historicalContext: 'Popularized by Rumelhart, Hinton, and Williams in 1986, though the algorithm was independently discovered multiple times.',
    tags: ['training', 'optimization', 'fundamental'] },
  { id: 'clustering', word: 'Clustering', category: 'Machine Learning', pronunciation: 'KLUS-tur-ing',
    explanation: { beginner: 'Clustering automatically groups similar items together without being told the groups — like sorting a bag of mixed candies by color without knowing the color names.', intermediate: 'Unsupervised learning technique that groups data points by similarity. K-means assigns points to k centroids. DBSCAN finds density-based clusters of arbitrary shape. Hierarchical clustering builds a dendrogram.', expert: 'K-means minimizes within-cluster sum of squares (Lloyd\'s algorithm). K-means++ initialization improves convergence. Spectral clustering uses graph Laplacian eigenvectors. HDBSCAN extends DBSCAN with variable density. Gaussian Mixture Models provide soft clustering with probabilistic assignments. Silhouette score and elbow method evaluate cluster quality.' },
    codeExample: {"language":"python","code":"from sklearn.cluster import KMeans\nkmeans = KMeans(n_clusters=3, random_state=0)\n# kmeans.fit(X)\n# labels = kmeans.labels_","description":"K-Means Clustering"},
    relatedTerms: ['k-means', 'unsupervised-learning', 'dimensionality-reduction'],
    historicalContext: 'K-means algorithm was proposed by Stuart Lloyd in 1957 (published 1982) at Bell Labs.',
    tags: ['unsupervised', 'grouping', 'analysis'] },
  { id: 'transformer', word: 'Transformer', category: 'Machine Learning', pronunciation: 'trans-FOR-mer',
    explanation: { beginner: 'A Transformer is the AI architecture behind ChatGPT and Google Translate — it processes all words simultaneously using "attention" to understand which words relate to each other.', intermediate: 'Transformers use self-attention mechanisms to process sequences in parallel (unlike sequential RNNs). Multi-head attention captures different relationship types. Positional encoding provides order information. Encoder-decoder structure for seq2seq tasks.', expert: 'Self-attention is O(n²) in sequence length — addressed by linear attention, sparse attention (Longformer), and Flash Attention (IO-aware). Pre-training (BERT: masked LM, GPT: autoregressive) enables transfer learning. Mixture of Experts scales parameters without proportional compute. RoPE and ALiBi handle positional encoding for long contexts.' },
    codeExample: {"language":"python","code":"from transformers import pipeline\nclassifier = pipeline(\"sentiment-analysis\")\nresult = classifier(\"I love building AI tools!\")\nprint(result) # [{'label': 'POSITIVE', 'score': 0.99}]","description":"Using a Transformer model via HuggingFace"},
    relatedTerms: ['attention-mechanism', 'bert', 'gpt', 'self-attention'],
    historicalContext: 'Introduced in "Attention Is All You Need" by Vaswani et al. at Google in 2017.',
    tags: ['architecture', 'attention', 'nlp', 'deep-learning'] },
  { id: 'reinforcement-learning', word: 'Reinforcement Learning', category: 'Machine Learning', pronunciation: 'ree-in-FORS-ment LUR-ning',
    explanation: { beginner: 'Reinforcement learning is how an AI learns by trial and error — like training a dog with treats. The AI takes actions, gets rewards or penalties, and learns what works.', intermediate: 'RL agents learn policies that maximize cumulative reward by interacting with an environment. Key concepts: state, action, reward, policy, value function. Methods: Q-learning (value-based), policy gradient, actor-critic.', expert: 'Deep RL: DQN (experience replay + target networks), PPO (clipped surrogate objective), SAC (maximum entropy). Model-based RL (MuZero, Dreamer) learns environment dynamics. Multi-agent RL, offline RL (from static datasets), and reward shaping address practical challenges. RLHF aligns language models with human preferences.' },
    codeExample: {"language":"python","code":"import gym\nenv = gym.make(\"CartPole-v1\")\nstate = env.reset()\nfor _ in range(100):\n    action = env.action_space.sample() # Random action\n    state, reward, done, info = env.step(action)\n    if done: break","description":"Interacting with an RL environment"},
    relatedTerms: ['q-learning', 'markov-decision-process', 'policy-gradient'],
    historicalContext: 'Roots in behavioral psychology. Modern RL formalized by Sutton and Barto. DeepMind\'s AlphaGo (2016) was a landmark achievement.',
    tags: ['paradigm', 'agent', 'reward'] },
  { id: 'decision-tree', word: 'Decision Tree', category: 'Machine Learning', pronunciation: 'deh-SIH-zhun tree',
    explanation: { beginner: 'A decision tree makes choices by asking a series of yes/no questions — like a flowchart that leads to an answer based on your responses.', intermediate: 'Decision trees recursively split data on features that maximize information gain (ID3) or minimize Gini impurity (CART). Interpretable and handles both numerical and categorical data. Prone to overfitting — pruning and ensembles help.', expert: 'Split criteria: information gain, gain ratio (C4.5), Gini impurity (CART). Pre-pruning (max depth, min samples) and post-pruning (reduced error, cost-complexity) control complexity. Oblique trees split on linear combinations. CART produces binary trees; C4.5/C5.0 allow multi-way splits.' },
    codeExample: {"language":"python","code":"from sklearn.tree import DecisionTreeClassifier\nclf = DecisionTreeClassifier(max_depth=3)\n# clf.fit(X_train, y_train)\n# print(clf.score(X_test, y_test))","description":"Decision Tree Classifier"},
    relatedTerms: ['random-forest', 'xgboost', 'ensemble-learning', 'classification'],
    historicalContext: 'ID3 algorithm developed by Ross Quinlan in 1986. CART by Breiman et al. in 1984.',
    tags: ['classification', 'interpretable', 'tree'] },
  { id: 'k-means', word: 'K-Means Clustering', category: 'Machine Learning', pronunciation: 'KAY meenz',
    explanation: { beginner: 'K-means groups data points into K clusters by finding K center points and assigning each data point to the nearest center, then adjusting the centers repeatedly.', intermediate: 'K-means iteratively assigns points to the nearest of K centroids, then recomputes centroids as cluster means. Converges when assignments stabilize. Requires choosing K in advance. Sensitive to initialization and outliers.', expert: 'K-means minimizes within-cluster sum of squares (NP-hard in general). K-means++ initialization achieves O(log k) competitive ratio. Mini-batch K-means scales to large datasets. Elkan\'s algorithm uses triangle inequality to skip distance computations. K-medoids (PAM) is more robust to outliers.' },
    codeExample: {"language":"python","code":"from sklearn.cluster import KMeans\n# Instantiate and fit\nkmeans = KMeans(n_clusters=3)\n# kmeans.fit(data)\n# centroids = kmeans.cluster_centers_","description":"K-Means finding centroids"},
    relatedTerms: ['clustering', 'unsupervised-learning', 'dbscan'],
    historicalContext: 'Proposed by Stuart Lloyd in 1957, independently by Hugo Steinhaus, and later by James MacQueen (1967) who coined the name.',
    tags: ['clustering', 'unsupervised', 'centroid'] },
  { id: 'cross-validation', word: 'Cross-Validation', category: 'Machine Learning', pronunciation: 'KROSS val-ih-DAY-shun',
    explanation: { beginner: 'Cross-validation tests how well a model will work on new data by splitting data into parts, training on some parts and testing on others, then rotating.', intermediate: 'K-fold cross-validation splits data into K folds, trains on K-1 folds and validates on the remaining fold, repeating K times. Provides more reliable performance estimates than a single train/test split. Stratified K-fold preserves class distribution.', expert: 'Leave-one-out CV (LOOCV) has lowest bias but highest variance and computational cost. Nested CV separates hyperparameter tuning from evaluation. Time-series CV uses expanding/sliding windows to respect temporal order. Group K-fold prevents data leakage when samples are grouped.' },
    relatedTerms: ['overfitting', 'train-test-split', 'hyperparameter-tuning'],
    historicalContext: 'Introduced by Seymour Geisser in 1975, building on earlier holdout methods.',
    tags: ['evaluation', 'validation', 'methodology'] }
];

export const dbTerms: Term[] = [
  { id: 'sql', word: 'SQL', category: 'Databases', pronunciation: 'SEE-kwul or ESS-kyoo-EL',
    explanation: { beginner: 'SQL is the language used to talk to databases — you can ask questions like "show me all customers from New York" and the database gives you the answer.', intermediate: 'SQL (Structured Query Language) is the standard language for relational databases. Core operations: SELECT (query), INSERT, UPDATE, DELETE. Supports JOINs, subqueries, aggregations, and transactions. DDL creates/modifies schema; DML manipulates data.', expert: 'SQL is based on relational algebra and tuple relational calculus. Query optimization uses cost-based optimizers with statistics. Window functions enable analytics without subqueries. CTEs (WITH clauses) and recursive CTEs simplify complex queries. SQL:2016 added JSON support. PostgreSQL extends SQL with rich types and PL/pgSQL.' },
    codeExample: { language: 'sql', code: 'SELECT c.name, COUNT(o.id) as order_count\nFROM customers c\nLEFT JOIN orders o ON c.id = o.customer_id\nWHERE c.created_at > \'2024-01-01\'\nGROUP BY c.name\nHAVING COUNT(o.id) > 5\nORDER BY order_count DESC;', description: 'SQL query with JOIN and aggregation' },
    relatedTerms: ['relational-database', 'normalization', 'indexing', 'acid'],
    historicalContext: 'Developed at IBM by Donald Chamberlin and Raymond Boyce in the early 1970s, based on Edgar Codd\'s relational model.',
    tags: ['language', 'query', 'relational'] },
  { id: 'normalization', word: 'Database Normalization', category: 'Databases', pronunciation: 'NOR-muh-lye-ZAY-shun',
    explanation: { beginner: 'Normalization organizes database tables to reduce repetition — instead of storing a customer\'s address in every order, you store it once and reference it.', intermediate: 'Normalization decomposes tables to eliminate redundancy and anomalies. Normal forms: 1NF (atomic values), 2NF (no partial dependencies), 3NF (no transitive dependencies), BCNF (every determinant is a candidate key). Trade-off between normalization and query performance.', expert: '4NF eliminates multi-valued dependencies, 5NF handles join dependencies. Denormalization improves read performance at cost of write complexity and consistency. Normalized schemas suit OLTP; star/snowflake schemas (dimensional modeling) suit OLAP. Functional dependency analysis drives decomposition.' },
    relatedTerms: ['sql', 'relational-database', 'acid', 'denormalization'],
    historicalContext: 'Introduced by Edgar Codd in 1970 as part of his relational model; higher normal forms developed through the 1970s-80s.',
    tags: ['design', 'schema', 'relational'] },
  { id: 'acid', word: 'ACID Properties', category: 'Databases', pronunciation: 'AS-id',
    explanation: { beginner: 'ACID ensures database operations are reliable — like a bank transfer that either fully completes or fully cancels, never leaving money in limbo.', intermediate: 'ACID: Atomicity (all or nothing), Consistency (valid state transitions), Isolation (concurrent transactions don\'t interfere), Durability (committed data survives crashes). Implemented via write-ahead logging, locking, and MVCC.', expert: 'Isolation levels: Read Uncommitted, Read Committed, Repeatable Read, Serializable — each trading consistency for concurrency. MVCC (Multi-Version Concurrency Control) provides snapshot isolation without locks. WAL (Write-Ahead Logging) ensures atomicity and durability. Two-phase commit (2PC) extends ACID to distributed systems. BASE (Basically Available, Soft state, Eventually consistent) is the alternative for distributed NoSQL systems.' },
    relatedTerms: ['transaction', 'sql', 'cap-theorem', 'mvcc'],
    historicalContext: 'The term ACID was coined by Andreas Reuter and Theo Härder in 1983, though the concepts were implemented earlier.',
    tags: ['reliability', 'transaction', 'consistency'] },
  { id: 'indexing', word: 'Database Indexing', category: 'Databases', pronunciation: 'IN-deks-ing',
    explanation: { beginner: 'A database index works like a book\'s index — instead of reading every page to find something, you look up the index to jump directly to the right page.', intermediate: 'Indexes create data structures (usually B-trees) on columns to speed up queries from O(n) table scan to O(log n) lookup. Types: primary, secondary, composite, unique. Trade-off: faster reads, slower writes, extra storage.', expert: 'Hash indexes for equality queries, B-tree for range queries, GiST for spatial/full-text, GIN for arrays/JSONB. Covering indexes include all query columns (index-only scans). Partial indexes filter rows. BRIN (Block Range INdex) for naturally ordered data. Index selection is an NP-hard problem; advisors use what-if analysis.' },
    relatedTerms: ['b-tree', 'sql', 'query-optimization', 'hash-table'],
    historicalContext: 'Database indexing evolved with relational databases in the 1970s; B-tree indexes became standard.',
    tags: ['performance', 'b-tree', 'query'] },
  { id: 'nosql', word: 'NoSQL', category: 'Databases', pronunciation: 'no-SEE-kwul',
    explanation: { beginner: 'NoSQL databases store data in flexible formats instead of rigid tables — like storing information in folders, documents, or key-value pairs instead of spreadsheets.', intermediate: 'NoSQL databases sacrifice some relational features for scalability and flexibility. Types: document (MongoDB), key-value (Redis), column-family (Cassandra), graph (Neo4j). Often eventually consistent (BASE) rather than ACID.', expert: 'NoSQL emerged from the need to scale horizontally beyond single-server relational limits. Document stores use BSON/JSON with flexible schemas. Wide-column stores model data as sparse tables. NewSQL (CockroachDB, Spanner) attempts to combine SQL + horizontal scalability. Multi-model databases support multiple paradigms.' },
    relatedTerms: ['sql', 'mongodb', 'redis', 'cap-theorem', 'sharding'],
    historicalContext: 'The term was first used by Carlo Strozzi in 1998. The modern NoSQL movement began around 2009 with the rise of web-scale applications.',
    tags: ['non-relational', 'scalability', 'flexible'] },
  { id: 'cap-theorem', word: 'CAP Theorem', category: 'Databases', pronunciation: 'KAP THEER-um',
    explanation: { beginner: 'The CAP theorem says a distributed database can only guarantee two of three things at once: Consistency, Availability, and tolerance to network Partitions. You have to choose which two matter most.', intermediate: 'In a distributed system, during a network partition you must choose between consistency (all nodes see the same data) and availability (every request gets a response). CP systems (HBase) sacrifice availability; AP systems (Cassandra) sacrifice consistency.', expert: 'CAP is often oversimplified — partitions are rare and the choice is nuanced. PACELC extends CAP: during Partition choose A/C; Else, choose Latency/Consistency. Jepsen testing validates consistency claims. Consensus protocols (Raft, Paxos) provide CP guarantees. CRDTs enable conflict-free AP replication.' },
    relatedTerms: ['acid', 'distributed-systems', 'consistency', 'sharding'],
    historicalContext: 'Conjectured by Eric Brewer in 2000, formally proven by Seth Gilbert and Nancy Lynch in 2002.',
    tags: ['distributed', 'theorem', 'trade-off'] },
  { id: 'sharding', word: 'Database Sharding', category: 'Databases', pronunciation: 'SHAR-ding',
    explanation: { beginner: 'Sharding splits a huge database across multiple servers — like dividing a phone book by last name initial so different servers handle different letters.', intermediate: 'Sharding horizontally partitions data across multiple database instances. Shard keys determine data distribution. Enables horizontal scaling but adds complexity: cross-shard queries, rebalancing, and distributed transactions.', expert: 'Sharding strategies: hash-based (even distribution), range-based (efficient range queries), directory-based (flexible mapping). Consistent hashing minimizes redistribution on node changes. Vitess (YouTube) and Citus (PostgreSQL) provide transparent sharding layers. Hot spots from skewed shard keys require careful key selection or auto-splitting.' },
    relatedTerms: ['cap-theorem', 'distributed-systems', 'horizontal-scaling', 'partition'],
    historicalContext: 'Popularized by Google\'s Bigtable (2006) and Amazon\'s Dynamo (2007) papers.',
    tags: ['scaling', 'distributed', 'partitioning'] },
  { id: 'orm', word: 'Object-Relational Mapping', category: 'Databases', pronunciation: 'oh-are-EM',
    explanation: { beginner: 'ORM lets you work with database data using programming objects instead of writing SQL — like talking to the database in your programming language instead of SQL.', intermediate: 'ORMs map database tables to classes and rows to objects. Examples: SQLAlchemy (Python), Hibernate (Java), Prisma (TypeScript). Simplify CRUD operations but can generate inefficient queries. N+1 query problem is a common pitfall.', expert: 'Active Record pattern (Rails) combines data access and domain logic. Data Mapper pattern (SQLAlchemy, Hibernate) separates them. Identity maps prevent duplicate object loading. Unit of Work tracks changes for batch commits. Query builders provide middle ground between raw SQL and full ORM. Lazy vs eager loading strategies affect performance significantly.' },
    relatedTerms: ['sql', 'relational-database', 'design-pattern'],
    historicalContext: 'The concept emerged in the 1990s with the growth of object-oriented programming and relational databases.',
    tags: ['abstraction', 'mapping', 'pattern'] }
];
