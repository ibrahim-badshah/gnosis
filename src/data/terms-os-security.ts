import type { Term } from './types';

export const osTerms: Term[] = [
  { id: 'process', word: 'Process', category: 'Operating Systems', pronunciation: 'PRAH-ses',
    explanation: { beginner: 'A process is a running program — when you open a browser or game, the OS creates a process for it with its own memory and resources.', intermediate: 'A process is an executing program with its own virtual address space, file descriptors, and execution state. OS manages process lifecycle: creation (fork/exec), scheduling, and termination. Inter-process communication via pipes, sockets, shared memory.', expert: 'Process control block (PCB) stores state, registers, page table pointer. Copy-on-write fork defers memory duplication. Process groups and sessions structure job control. cgroups (Linux) limit and account resource usage. Namespaces provide isolation (containers). The /proc filesystem exposes kernel process data.' },
    codeExample: {"language":"bash","code":"# List running processes\nps aux\n# Kill a process by PID\nkill -9 1234","description":"Process management in Unix"},
    relatedTerms: ['thread', 'scheduling', 'context-switching', 'memory-management'],
    historicalContext: 'The concept of processes was introduced in the Multics operating system in the 1960s.',
    tags: ['execution', 'fundamental', 'isolation'] },
  { id: 'thread', word: 'Thread', category: 'Operating Systems', pronunciation: 'THRED',
    explanation: { beginner: 'A thread is like a worker within a process — multiple threads in the same process share memory and work on different tasks simultaneously.', intermediate: 'Threads are lightweight execution units within a process, sharing the same address space. Enable parallelism and concurrent I/O handling. Types: kernel threads (OS-managed) and user threads (library-managed). Synchronization needed for shared data.', expert: 'POSIX threads (pthreads) are the standard. Green threads (goroutines, Java virtual threads) are user-space scheduled — M:N mapping to kernel threads. Thread-local storage provides per-thread globals. Lock-free programming uses atomics and CAS. Thread pools amortize creation cost. NUMA-aware threading optimizes memory access.' },
    codeExample: {"language":"python","code":"import threading\ndef worker():\n    print(\"Thread running\")\nt = threading.Thread(target=worker)\nt.start()\nt.join()","description":"Spawning a thread in Python"},
    relatedTerms: ['process', 'concurrency', 'mutex', 'deadlock'],
    historicalContext: 'Thread concept developed in the 1960s-70s; POSIX threads standardized in 1995.',
    tags: ['execution', 'concurrency', 'lightweight'] },
  { id: 'deadlock', word: 'Deadlock', category: 'Operating Systems', pronunciation: 'DED-lok',
    explanation: { beginner: 'Deadlock is when two processes are each waiting for the other to finish — like two cars meeting on a one-lane bridge, neither can move until the other backs up.', intermediate: 'Deadlock occurs when four conditions hold simultaneously: mutual exclusion, hold and wait, no preemption, circular wait. Prevention strategies: ordering lock acquisition, timeouts, lock-free algorithms. Detection via resource allocation graphs.', expert: 'Coffman conditions are necessary and sufficient. Prevention eliminates one condition: lock ordering prevents circular wait. Banker\'s algorithm (Dijkstra) avoids unsafe states. Detection uses cycle-finding in wait-for graphs. Database deadlock detection uses waits-for graph with victim selection. Priority inheritance prevents priority inversion deadlocks.' },
    relatedTerms: ['mutex', 'semaphore', 'concurrency', 'thread'],
    historicalContext: 'Formally studied by Dijkstra in the 1960s, named after traffic deadlocks.',
    tags: ['concurrency', 'problem', 'synchronization'] },
  { id: 'virtual-memory', word: 'Virtual Memory', category: 'Operating Systems', pronunciation: 'VUR-choo-ul MEM-uh-ree',
    explanation: { beginner: 'Virtual memory lets each program think it has its own private memory space, even if the physical RAM is shared — like giving everyone their own "address book" that maps to shared storage.', intermediate: 'Virtual memory maps virtual addresses to physical addresses via page tables. Enables memory isolation, memory-mapped files, and using disk as extended RAM (swapping). Page faults trigger loading pages from disk. TLB caches translations.', expert: 'Multi-level page tables reduce memory overhead for sparse address spaces. Huge pages (2MB/1GB) reduce TLB misses. Demand paging loads pages lazily. Page replacement algorithms: LRU (approximated with clock algorithm), working set model. ASLR randomizes address layout for security. Memory-mapped I/O unifies file and memory access.' },
    relatedTerms: ['paging', 'process', 'memory-management', 'cache'],
    historicalContext: 'Developed for the Atlas computer at Manchester University in 1962 by Tom Kilburn\'s team.',
    tags: ['memory', 'abstraction', 'paging'] },
  { id: 'scheduling', word: 'CPU Scheduling', category: 'Operating Systems', pronunciation: 'SHED-yoo-ling',
    explanation: { beginner: 'CPU scheduling decides which program gets to use the processor next — like a teacher deciding which student gets to answer the next question.', intermediate: 'Scheduling algorithms allocate CPU time to processes/threads. Types: FCFS (first come first served), SJF (shortest job first), Round Robin (time slices), Priority, Multilevel Feedback Queue. Goals: fairness, throughput, latency.', expert: 'Linux CFS (Completely Fair Scheduler) uses red-black tree of virtual runtimes. EEVDF (Earliest Eligible Virtual Deadline First) replaced CFS in Linux 6.6. Real-time scheduling: Rate Monotonic (static priority), EDF (dynamic). Gang scheduling runs related threads together. Work-stealing balances parallel tasks across cores.' },
    relatedTerms: ['process', 'thread', 'context-switching', 'round-robin'],
    historicalContext: 'CPU scheduling became necessary with the advent of time-sharing systems in the early 1960s.',
    tags: ['cpu', 'resource', 'algorithm'] },
  { id: 'file-system', word: 'File System', category: 'Operating Systems', pronunciation: 'FY-ul SIS-tum',
    explanation: { beginner: 'A file system organizes data on storage devices into files and folders — like organizing papers in a filing cabinet with labeled folders.', intermediate: 'File systems manage how data is stored and retrieved. Key concepts: inodes (metadata), directories, journaling (crash recovery). Types: ext4, NTFS, APFS, ZFS, Btrfs. Virtual File System layer provides a uniform API.', expert: 'Journaling (ext4, NTFS) logs metadata changes for crash recovery. Copy-on-write (ZFS, Btrfs) provides snapshots and checksumming. Log-structured file systems (F2FS) optimize for flash storage. FUSE enables user-space file systems. Distributed file systems (HDFS, CephFS) span clusters. io_uring provides efficient async I/O on Linux.' },
    relatedTerms: ['process', 'virtual-memory', 'disk-storage', 'inode'],
    historicalContext: 'Unix file system (1969) established the hierarchical directory structure used by most modern OSes.',
    tags: ['storage', 'organization', 'fundamental'] },
  { id: 'mutex', word: 'Mutex', category: 'Operating Systems', pronunciation: 'MYOO-teks',
    explanation: { beginner: 'A mutex is like a bathroom lock — only one person (thread) can use the resource at a time. Others wait until it\'s unlocked.', intermediate: 'A mutex (mutual exclusion) is a synchronization primitive that ensures only one thread accesses a critical section at a time. Lock/unlock operations. Recursive mutexes allow re-locking by the same thread. Deadlock can occur with improper ordering.', expert: 'Mutexes use atomic compare-and-swap (CAS) instructions. Spin locks are efficient for short critical sections. Adaptive mutexes spin briefly then sleep. Reader-writer locks allow concurrent reads. Priority ceiling and priority inheritance prevent priority inversion. Futex (Linux) minimizes syscalls for uncontended cases.' },
    codeExample: {"language":"python","code":"import threading\nlock = threading.Lock()\nglobal_counter = 0\ndef safe_increment():\n    global global_counter\n    with lock:\n        global_counter += 1","description":"Using a Mutex lock"},
    relatedTerms: ['semaphore', 'deadlock', 'thread', 'concurrency'],
    historicalContext: 'The concept of mutual exclusion was first discussed by Dijkstra in 1965.',
    tags: ['synchronization', 'concurrency', 'lock'] },
  { id: 'context-switching', word: 'Context Switching', category: 'Operating Systems', pronunciation: 'KON-tekst SWICH-ing',
    explanation: { beginner: 'Context switching is when the CPU saves one program\'s state and loads another\'s — like a chef pausing one recipe to work on another, remembering where they left off.', intermediate: 'Context switching saves a process/thread\'s register state and loads another\'s. Involves saving/restoring CPU registers, program counter, stack pointer, and switching address spaces. Costly due to cache invalidation and TLB flushes.', expert: 'Typical context switch cost: 1-10 microseconds. Voluntary (I/O wait, yield) vs involuntary (preemption). User-space context switching (green threads, coroutines) is much cheaper — no kernel transition. Tagged TLBs (PCID/ASID) avoid full TLB flushes. Direct process switch skips saving state to memory when possible.' },
    relatedTerms: ['process', 'thread', 'scheduling', 'cache'],
    historicalContext: 'Context switching has been part of multitasking systems since the earliest time-sharing systems in the 1960s.',
    tags: ['cpu', 'performance', 'multitasking'] }
];

export const securityTerms: Term[] = [
  { id: 'encryption', word: 'Encryption', category: 'Security', pronunciation: 'en-KRIP-shun',
    explanation: { beginner: 'Encryption scrambles data so only authorized people can read it — like writing a secret message in a code that only your friend knows how to decode.', intermediate: 'Encryption transforms plaintext into ciphertext using a key and algorithm. Symmetric encryption (AES) uses one key; asymmetric (RSA) uses public/private key pairs. Used for data at rest, in transit (TLS), and end-to-end (Signal).', expert: 'AES-256-GCM provides authenticated encryption (AEAD). RSA key sizes ≥ 2048 bits for security. Elliptic curve cryptography (ECC) provides equivalent security with smaller keys. Post-quantum cryptography (CRYSTALS-Kyber, Dilithium) addresses quantum computing threats. Homomorphic encryption enables computation on encrypted data.' },
    relatedTerms: ['aes', 'rsa', 'tls', 'hashing'],
    historicalContext: 'Ancient civilizations used ciphers; modern cryptography was revolutionized by Diffie-Hellman (1976) and RSA (1977).',
    tags: ['fundamental', 'confidentiality', 'cipher'] },
  { id: 'xss', word: 'Cross-Site Scripting', category: 'Security', pronunciation: 'KROSS-syt SKRIP-ting',
    explanation: { beginner: 'XSS is when an attacker injects malicious code into a website that runs in other users\' browsers — like someone slipping a fake page into a book that tricks readers.', intermediate: 'XSS occurs when user input is rendered as HTML/JS without sanitization. Types: Stored (persisted in DB), Reflected (in URL params), DOM-based (client-side). Prevention: output encoding, Content Security Policy (CSP), sanitization libraries.', expert: 'CSP headers restrict script sources (nonces or hashes for inline scripts). Trusted Types prevent DOM XSS by requiring typed objects. HttpOnly cookies prevent XSS from stealing session tokens. DOMPurify sanitizes HTML safely. Mutation XSS exploits browser parser differences. SubResource Integrity (SRI) prevents CDN compromise.' },
    codeExample: {"language":"javascript","code":"// Bad: elem.innerHTML = userInput;\n// Good (prevents XSS):\nconst elem = document.createElement(\"div\");\nelem.textContent = \"<script>alert(1)</script>\";\n// Or using a sanitizer library:\n// elem.innerHTML = DOMPurify.sanitize(userInput);","description":"Preventing XSS in the DOM"},
    relatedTerms: ['csrf', 'sql-injection', 'cors', 'csp'],
    historicalContext: 'Coined by Microsoft engineers in 2000; has been in the OWASP Top 10 since its inception.',
    tags: ['vulnerability', 'web', 'injection'] },
  { id: 'sql-injection', word: 'SQL Injection', category: 'Security', pronunciation: 'SEE-kwul in-JEK-shun',
    explanation: { beginner: 'SQL injection is when a hacker types SQL commands into a website form to trick the database — like writing "AND give me all passwords" in a search box.', intermediate: 'SQL injection exploits unsanitized user input in SQL queries. Attacker inserts SQL code to bypass authentication, extract data, or modify the database. Prevention: parameterized queries (prepared statements), ORMs, input validation.', expert: 'Blind SQL injection infers data through boolean or time-based responses. Second-order injection stores payload for later execution. WAFs provide defense in depth but can be bypassed. Parameterized queries (binding values, not concatenating) are the primary defense. Database least privilege and stored procedures provide additional layers.' },
    codeExample: { language: 'python', code: '# VULNERABLE - never do this!\nquery = f"SELECT * FROM users WHERE name = \'{user_input}\'"  \n\n# SAFE - use parameterized queries\ncursor.execute("SELECT * FROM users WHERE name = %s", (user_input,))', description: 'SQL injection prevention' },
    relatedTerms: ['xss', 'security', 'sql', 'prepared-statements'],
    historicalContext: 'First publicly discussed by Jeff Forristal in Phrack Magazine in 1998.',
    tags: ['vulnerability', 'database', 'injection'] },
  { id: 'jwt', word: 'JSON Web Token', category: 'Security', pronunciation: 'JOT',
    explanation: { beginner: 'JWT is a compact, self-contained token for securely transmitting information — like a tamper-evident ID card that contains your credentials.', intermediate: 'JWTs have three Base64-encoded parts: header (algorithm), payload (claims), and signature. Used for stateless authentication. JWS (signed) verifies integrity; JWE (encrypted) provides confidentiality. Common algorithms: HS256, RS256.', expert: 'JWT pitfalls: algorithm confusion attacks (accepting "none"), key confusion (HMAC key used with RSA), no built-in revocation. Best practices: short expiry + refresh tokens, audience/issuer validation, use asymmetric keys (RS256/ES256). PASETO is a safer alternative. Token binding prevents token theft.' },
    codeExample: {"language":"javascript","code":"const jwt = require(\"jsonwebtoken\");\nconst token = jwt.sign({ userId: 123 }, \"secret_key\", { expiresIn: \"1h\" });\nconst decoded = jwt.verify(token, \"secret_key\");","description":"Creating and verifying a JWT"},
    relatedTerms: ['oauth', 'authentication', 'encryption', 'session'],
    historicalContext: 'JWT was proposed as RFC 7519 in 2015 by Michael Jones, John Bradley, and Nat Sakimura.',
    tags: ['token', 'authentication', 'standard'] },
  { id: 'hashing', word: 'Hashing', category: 'Security', pronunciation: 'HASH-ing',
    explanation: { beginner: 'Hashing converts data into a fixed-length fingerprint — like summarizing a book into a unique code. Even a tiny change produces a completely different hash.', intermediate: 'Hash functions map arbitrary input to fixed-size output. Cryptographic hashes (SHA-256) are one-way and collision-resistant. Used for password storage (bcrypt, argon2), data integrity, and digital signatures. Non-cryptographic hashes (MurmurHash) are faster for hash tables.', expert: 'Password hashing requires: slow computation (bcrypt cost factor), salt (per-password random), and memory-hardness (Argon2id). SHA-256 has 128-bit collision resistance. Length extension attacks affect SHA-256 but not SHA-3. HMAC uses hash functions for message authentication. Merkle trees chain hashes for efficient integrity verification (used in Bitcoin, Git).' },
    relatedTerms: ['encryption', 'hash-table', 'sha-256', 'digital-signature'],
    historicalContext: 'MD5 designed by Ronald Rivest (1991), now broken. SHA-1 (1995) deprecated. SHA-256 (2001) and SHA-3 (2015) are current standards.',
    tags: ['integrity', 'one-way', 'fundamental'] },
  { id: 'tls', word: 'TLS/SSL', category: 'Security', pronunciation: 'TEE-EL-ESS',
    explanation: { beginner: 'TLS encrypts the connection between your browser and a website — the padlock icon in your address bar means TLS is protecting your data.', intermediate: 'Transport Layer Security provides encryption, authentication, and integrity for network communication. TLS handshake negotiates cipher suite and exchanges keys. Certificate authorities verify server identity. TLS 1.3 reduced handshake to one round-trip.', expert: 'TLS 1.3 removed vulnerable ciphers (RC4, CBC), static RSA key exchange, and custom DHE groups. 0-RTT resumption enables zero round-trip connections (with replay risk). Certificate Transparency logs prevent rogue certificates. mTLS (mutual TLS) authenticates both client and server. OCSP stapling provides efficient certificate revocation checking.' },
    relatedTerms: ['encryption', 'http', 'certificate', 'handshake'],
    historicalContext: 'SSL created by Netscape (1995). Renamed TLS for standardization (1999). TLS 1.3 published in 2018.',
    tags: ['protocol', 'transport', 'certificate'] }
];
