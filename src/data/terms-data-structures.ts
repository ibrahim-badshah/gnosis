import type { Term } from './types';

export const dataStructureTerms: Term[] = [
  { id: 'array', word: 'Array', category: 'Data Structures', pronunciation: 'uh-RAY',
    explanation: { beginner: 'An array is a collection of items stored in a row, like numbered boxes in a shelf. You can quickly access any item by its position number (index).', intermediate: 'Arrays store elements in contiguous memory locations, enabling O(1) random access by index. Fixed-size in most languages; dynamic arrays (ArrayList, vector) resize by doubling. Insert/delete at arbitrary positions is O(n) due to shifting.', expert: 'Arrays offer optimal cache locality due to contiguous storage. Dynamic arrays amortize append to O(1) via geometric growth (typically 2x). Sparse arrays use hash maps or trees. SIMD operations exploit array layout. Gap buffers optimize insertions at cursor positions (text editors).' },
    codeExample: { language: 'python', code: '# Array operations\narr = [10, 20, 30, 40, 50]\nprint(arr[2])      # O(1) access: 30\narr.append(60)     # O(1) amortized\narr.insert(1, 15)  # O(n) shift', description: 'Basic array operations' },
    relatedTerms: ['linked-list', 'dynamic-array', 'hash-table', 'matrix'],
    historicalContext: 'Arrays are one of the oldest data structures, implemented in the earliest programming languages like Fortran (1957).',
    tags: ['linear', 'sequential', 'fundamental'] },
  { id: 'linked-list', word: 'Linked List', category: 'Data Structures', pronunciation: 'LINKT list',
    explanation: { beginner: 'A linked list is like a chain where each link holds a value and points to the next link. You follow the chain to find items.', intermediate: 'A linked list stores elements in nodes, each containing data and a pointer to the next node. Supports O(1) insert/delete at head. No random access — traversal is O(n). Variants: singly, doubly, circular linked lists.', expert: 'Linked lists have poor cache performance vs arrays. XOR linked lists save space using XOR of prev/next pointers. Skip lists layer multiple linked lists for O(log n) search. Intrusive linked lists embed nodes in the data structure. Lock-free linked lists enable concurrent access.' },
    codeExample: { language: 'python', code: 'class Node:\n    def __init__(self, val, next=None):\n        self.val = val\n        self.next = next\n\nhead = Node(1, Node(2, Node(3)))\n# Traverse\ncurr = head\nwhile curr:\n    print(curr.val)\n    curr = curr.next', description: 'Simple linked list' },
    relatedTerms: ['array', 'stack', 'queue', 'doubly-linked-list'],
    historicalContext: 'Invented by Allen Newell, Cliff Shaw, and Herbert A. Simon at RAND Corporation in 1955-56.',
    tags: ['linear', 'pointer', 'dynamic'] },
  { id: 'stack', word: 'Stack', category: 'Data Structures', pronunciation: 'STAK',
    explanation: { beginner: 'A stack is like a pile of plates — you can only add or remove from the top. Last in, first out (LIFO).', intermediate: 'A stack supports push (add to top) and pop (remove from top) in O(1). Used for function call management, expression evaluation, undo operations, and DFS. Can be implemented with arrays or linked lists.', expert: 'The call stack manages function frames, return addresses, and local variables. Stack-based VMs (JVM, CPython) use operand stacks. Min-stack maintains minimum in O(1) using auxiliary stack. Cactus stacks support multiple heads for coroutines/continuations.' },
    codeExample: {"language":"javascript","code":"class Stack {\n  constructor() { this.items = []; }\n  push(elem) { this.items.push(elem); }\n  pop() { return this.items.pop(); }\n  peek() { return this.items[this.items.length - 1]; }\n  isEmpty() { return this.items.length === 0; }\n}","description":"Simple Stack in JavaScript"},
    relatedTerms: ['queue', 'dfs', 'recursion', 'call-stack'],
    historicalContext: 'Proposed by Alan Turing in 1946 and independently by Friedrich Bauer and Klaus Samelson in 1955.',
    tags: ['linear', 'lifo', 'fundamental'] },
  { id: 'queue', word: 'Queue', category: 'Data Structures', pronunciation: 'KYOO',
    explanation: { beginner: 'A queue is like a line at a store — the first person in line is served first. First in, first out (FIFO).', intermediate: 'Queues support enqueue (add to back) and dequeue (remove from front) in O(1). Implemented with circular arrays or linked lists. Variants: deque (double-ended), priority queue, circular queue.', expert: 'Lock-free queues (Michael-Scott) enable concurrent access. Work-stealing deques power task schedulers. Disruptor pattern uses ring buffers for ultra-low-latency messaging. Priority queues with decrease-key support Dijkstra\'s algorithm efficiently.' },
    codeExample: {"language":"javascript","code":"class Queue {\n  constructor() { this.items = []; }\n  enqueue(elem) { this.items.push(elem); }\n  dequeue() { return this.items.shift(); }\n  front() { return this.items[0]; }\n  isEmpty() { return this.items.length === 0; }\n}","description":"Queue using JavaScript Array"},
    relatedTerms: ['stack', 'bfs', 'priority-queue', 'deque'],
    historicalContext: 'Formalized as an abstract data type in the early days of computing for job scheduling.',
    tags: ['linear', 'fifo', 'fundamental'] },
  { id: 'binary-search-tree', word: 'Binary Search Tree', category: 'Data Structures', pronunciation: 'BY-nuh-ree SURCH tree',
    explanation: { beginner: 'A BST is a tree where each node\'s left children are smaller and right children are larger — making it quick to find, add, or remove values.', intermediate: 'BST supports search, insert, delete in O(h) where h is height. In balanced BSTs (AVL, Red-Black), h = O(log n). In-order traversal produces sorted output. Unbalanced BSTs degrade to O(n).', expert: 'Self-balancing BSTs maintain O(log n) height: AVL trees (strict balance, faster lookups), Red-Black trees (relaxed, faster inserts), splay trees (amortized, move-to-root). Treaps combine BSTs with heaps using random priorities. BSTs support order statistics with augmentation.' },
    codeExample: { language: 'python', code: 'class TreeNode:\n    def __init__(self, val):\n        self.val = val\n        self.left = self.right = None\n\ndef insert(root, val):\n    if not root: return TreeNode(val)\n    if val < root.val:\n        root.left = insert(root.left, val)\n    else:\n        root.right = insert(root.right, val)\n    return root', description: 'BST insert' },
    relatedTerms: ['avl-tree', 'red-black-tree', 'binary-tree', 'binary-search'],
    historicalContext: 'Independently discovered by multiple researchers in the 1960s as a way to maintain sorted dynamic data.',
    tags: ['tree', 'sorted', 'hierarchical'] },
  { id: 'hash-table', word: 'Hash Table', category: 'Data Structures', pronunciation: 'HASH TAY-bul',
    explanation: { beginner: 'A hash table is like a dictionary where you look up a word (key) to find its definition (value) instantly. A special formula converts keys into locations.', intermediate: 'Hash tables map keys to values using a hash function. Average O(1) for get/set/delete. Collision resolution: chaining (linked lists) or open addressing (linear/quadratic probing, double hashing). Load factor affects performance.', expert: 'Perfect hashing achieves worst-case O(1). Cuckoo hashing guarantees O(1) lookups. Robin Hood hashing reduces variance. Consistent hashing distributes keys across nodes. Swiss table (Google) and F14 (Facebook) optimize for modern CPUs with SIMD probing. Extendible and linear hashing support dynamic growth.' },
    codeExample: {"language":"javascript","code":"const hashTable = new Map();\nhashTable.set(\"key1\", \"value1\");\nhashTable.set(\"key2\", \"value2\");\nconsole.log(hashTable.get(\"key1\")); // Output: value1\nconsole.log(hashTable.has(\"key3\")); // Output: false","description":"Using JS Map as a Hash Table"},
    relatedTerms: ['hash-function', 'array', 'set', 'collision-resolution'],
    historicalContext: 'Hash tables were first described by Hans Peter Luhn in an IBM memo in 1953.',
    tags: ['key-value', 'hashing', 'fundamental'] },
  { id: 'heap', word: 'Heap', category: 'Data Structures', pronunciation: 'HEEP',
    explanation: { beginner: 'A heap is a special tree where the parent is always bigger (or smaller) than its children — like a tournament bracket where the winner is always at the top.', intermediate: 'A binary heap is a complete binary tree satisfying the heap property (max-heap: parent ≥ children). Supports insert and extract-min/max in O(log n). Efficiently implemented as an array. Used in priority queues and heapsort.', expert: 'Fibonacci heaps offer O(1) amortized insert and decrease-key, O(log n) extract-min — optimal for Dijkstra\'s. Binomial heaps support O(log n) merge. Pairing heaps are simpler with good practical performance. d-ary heaps trade off height vs branching factor for cache efficiency.' },
    codeExample: {"language":"python","code":"import heapq\nmin_heap = []\nheapq.heappush(min_heap, 5)\nheapq.heappush(min_heap, 1)\nheapq.heappush(min_heap, 3)\nprint(heapq.heappop(min_heap)) # Output: 1","description":"Min-Heap in Python"},
    relatedTerms: ['priority-queue', 'heap-sort', 'binary-tree', 'fibonacci-heap'],
    historicalContext: 'Introduced by J.W.J. Williams in 1964 alongside heapsort.',
    tags: ['tree', 'priority', 'complete'] },
  { id: 'graph', word: 'Graph', category: 'Data Structures', pronunciation: 'GRAF',
    explanation: { beginner: 'A graph is a set of points (nodes) connected by lines (edges) — like a social network where people are nodes and friendships are edges.', intermediate: 'Graphs consist of vertices and edges. Can be directed/undirected, weighted/unweighted, cyclic/acyclic. Representations: adjacency matrix (O(V²) space, O(1) lookup), adjacency list (O(V+E) space). Fundamental for modeling relationships.', expert: 'Graph problems span P to NP-hard. Planar graphs satisfy Euler\'s formula V-E+F=2. Spectral graph theory uses eigenvalues of adjacency/Laplacian matrices. Graph databases (Neo4j) use property graph model. Hypergraphs generalize to edges connecting multiple vertices.' },
    codeExample: {"language":"javascript","code":"class Graph {\n  constructor() { this.adjList = new Map(); }\n  addVertex(v) { this.adjList.set(v, []); }\n  addEdge(v, w) { this.adjList.get(v).push(w); this.adjList.get(w).push(v); }\n}","description":"Adjacency List Graph in JS"},
    relatedTerms: ['bfs', 'dfs', 'dijkstra', 'adjacency-list', 'tree'],
    historicalContext: 'Graph theory began with Euler\'s solution to the Königsberg bridge problem in 1736.',
    tags: ['non-linear', 'relationships', 'fundamental'] },
  { id: 'trie', word: 'Trie', category: 'Data Structures', pronunciation: 'TRY',
    explanation: { beginner: 'A trie is a tree where each branch represents a letter, and following branches from root to leaf spells out a word — perfect for autocomplete features.', intermediate: 'A trie (prefix tree) stores strings character by character, with shared prefixes sharing nodes. Search, insert, delete are O(m) where m is string length. Supports prefix queries efficiently. Memory-intensive but fast.', expert: 'Compressed tries (Patricia/radix tries) merge single-child chains. Ternary search tries balance memory and speed. HAT-tries cache-efficiently store strings. Burst tries switch from trie to sorted arrays at leaves. Suffix tries/trees enable O(m) substring search. DAFSA (directed acyclic word graph) minimizes trie nodes.' },
    codeExample: {"language":"javascript","code":"class TrieNode {\n  constructor() { this.children = {}; this.isEndOfWord = false; }\n}\nclass Trie {\n  constructor() { this.root = new TrieNode(); }\n  insert(word) {\n    let curr = this.root;\n    for(let char of word) {\n      if(!curr.children[char]) curr.children[char] = new TrieNode();\n      curr = curr.children[char];\n    }\n    curr.isEndOfWord = true;\n  }\n}","description":"Trie Data Structure in JS"},
    relatedTerms: ['hash-table', 'binary-search-tree', 'suffix-tree', 'autocomplete'],
    historicalContext: 'Invented by Edward Fredkin in 1960; the name comes from "retrieval" though it\'s pronounced "try".',
    tags: ['tree', 'string', 'prefix'] },
  { id: 'red-black-tree', word: 'Red-Black Tree', category: 'Data Structures', pronunciation: 'RED blak tree',
    explanation: { beginner: 'A red-black tree is a self-balancing tree that uses color rules (red and black nodes) to stay roughly balanced, ensuring fast operations.', intermediate: 'Red-black trees maintain balance through 5 properties involving node coloring. All operations are O(log n). Rotations and recoloring restore balance after inserts/deletes. Less strictly balanced than AVL trees but require fewer rotations.', expert: 'RB-trees guarantee height ≤ 2·log₂(n+1). Used in Linux CFS scheduler, Java TreeMap, C++ std::map. Left-leaning red-black trees (LLRB) simplify implementation. 2-3-4 trees are isomorphic to RB-trees. AA trees (Arne Andersson) simplify by allowing only right-leaning red links.' },
    codeExample: {"language":"python","code":"# Complex to implement fully, but widely used internally.\nimport bisect\nclass SimpleBalancedBST:\n    def __init__(self): self.arr = []\n    def insert(self, val): bisect.insort(self.arr, val)\n    def contains(self, val):\n        i = bisect.bisect_left(self.arr, val)\n        return i != len(self.arr) and self.arr[i] == val","description":"Using bisect as a lightweight balanced tree substitute in Python"},
    relatedTerms: ['avl-tree', 'binary-search-tree', 'b-tree', 'splay-tree'],
    historicalContext: 'Invented by Rudolf Bayer in 1972 as "symmetric binary B-trees", named red-black by Guibas and Sedgewick in 1978.',
    tags: ['tree', 'balanced', 'self-balancing'] },
  { id: 'b-tree', word: 'B-Tree', category: 'Data Structures', pronunciation: 'BEE tree',
    explanation: { beginner: 'A B-tree is a wide, short tree designed for reading and writing large blocks of data — used by databases and file systems to find data on hard drives quickly.', intermediate: 'B-trees are self-balancing multi-way search trees optimized for disk I/O. Each node can have many keys and children, minimizing tree height and disk reads. All leaves are at the same depth. B+ trees store data only in leaves.', expert: 'B-trees minimize I/O operations by matching node size to disk page size. B+ trees are preferred for databases (sequential leaf scanning via linked leaves). B* trees increase minimum occupancy to 2/3. Fractal trees (Bε-trees) optimize write performance. LSM-trees offer even better write throughput at the cost of read amplification.' },
    relatedTerms: ['binary-search-tree', 'red-black-tree', 'database-indexing', 'disk-storage'],
    historicalContext: 'Invented by Rudolf Bayer and Edward McCreight at Boeing Research Labs in 1970.',
    tags: ['tree', 'disk', 'database', 'balanced'] },
  { id: 'avl-tree', word: 'AVL Tree', category: 'Data Structures', pronunciation: 'AY-vee-EL tree',
    explanation: { beginner: 'An AVL tree is a binary search tree that automatically rebalances itself after every insert or delete, ensuring fast lookups always.', intermediate: 'AVL trees maintain strict balance: the height difference between left and right subtrees of any node is at most 1. Rebalanced via rotations (single/double). All operations O(log n). Faster lookups than red-black trees but slower inserts.', expert: 'AVL trees have height ≤ 1.44·log₂(n+2). Minimum nodes for height h: N(h) = N(h-1) + N(h-2) + 1 (Fibonacci-like). Require O(log n) rotations per insert (worst case), but only O(1) rotations per delete. Preferred for lookup-heavy workloads.' },
    relatedTerms: ['red-black-tree', 'binary-search-tree', 'rotation', 'balanced-tree'],
    historicalContext: 'Invented by Georgy Adelson-Velsky and Evgenii Landis in 1962 — the first self-balancing BST.',
    tags: ['tree', 'balanced', 'self-balancing'] },
  { id: 'priority-queue', word: 'Priority Queue', category: 'Data Structures', pronunciation: 'pry-OR-ih-tee KYOO',
    explanation: { beginner: 'A priority queue is like an emergency room — patients aren\'t served in arrival order but by urgency. The most urgent item comes out first.', intermediate: 'Priority queues support insert and extract-min/max efficiently. Typically implemented with binary heaps (O(log n) for both). Used in Dijkstra\'s algorithm, Huffman coding, task scheduling, and A* search.', expert: 'Fibonacci heaps give O(1) amortized insert and decrease-key. van Emde Boas trees achieve O(log log U) for integer keys. Brodal queues provide worst-case O(1) insert and O(log n) extract-min. Parallel priority queues use concurrent skip lists or relaxed structures.' },
    codeExample: {"language":"python","code":"import queue\npq = queue.PriorityQueue()\npq.put((2, \"Task B\"))\npq.put((1, \"Task A\"))\nprint(pq.get()) # Output: (1, \"Task A\")","description":"Priority Queue using Python queue module"},
    relatedTerms: ['heap', 'queue', 'dijkstra', 'scheduling'],
    historicalContext: 'The abstract concept predates specific implementations; binary heap implementation by Williams (1964).',
    tags: ['abstract', 'priority', 'scheduling'] },
  { id: 'disjoint-set', word: 'Union-Find', category: 'Data Structures', pronunciation: 'YOON-yun FYND',
    explanation: { beginner: 'Union-Find tracks groups of items. You can ask "are these two items in the same group?" and merge groups together, like managing teams that sometimes combine.', intermediate: 'Disjoint Set Union (DSU) supports find (which set contains an element) and union (merge two sets). With path compression and union by rank, both operations are nearly O(1) — amortized O(α(n)) where α is the inverse Ackermann function.', expert: 'The inverse Ackermann function α(n) ≤ 4 for any practical n, making DSU effectively O(1). Used in Kruskal\'s MST, connected components, percolation. Weighted quick-union with path halving is simpler and equally efficient. Persistent DSU enables rollback. Link-cut trees generalize DSU for dynamic tree connectivity.' },
    codeExample: {"language":"python","code":"class DisjointSet:\n    def __init__(self, n):\n        self.parent = list(range(n))\n    def find(self, i):\n        if self.parent[i] == i: return i\n        self.parent[i] = self.find(self.parent[i])\n        return self.parent[i]\n    def union(self, i, j):\n        root_i, root_j = self.find(i), self.find(j)\n        if root_i != root_j: self.parent[root_i] = root_j","description":"Disjoint Set (Union-Find) with Path Compression"},
    relatedTerms: ['kruskal', 'connected-components', 'graph'],
    historicalContext: 'Developed by Bernard Galler and Michael Fischer in 1964. Path compression analyzed by Tarjan in 1975.',
    tags: ['set', 'grouping', 'graph'] },
  { id: 'bloom-filter', word: 'Bloom Filter', category: 'Data Structures', pronunciation: 'BLOOM FIL-ter',
    explanation: { beginner: 'A Bloom filter can quickly tell you "definitely not in the set" or "probably in the set" — like a bouncer who sometimes lets in the wrong person but never turns away the right one.', intermediate: 'A Bloom filter is a space-efficient probabilistic data structure for set membership queries. Uses multiple hash functions mapping to a bit array. No false negatives, but has a tunable false positive rate. Cannot delete elements (counting Bloom filters can).', expert: 'Optimal number of hash functions k = (m/n)·ln(2) for m bits and n elements. False positive rate ≈ (1-e^(-kn/m))^k. Variants: counting Bloom filters (deletion), cuckoo filters (deletion + lower FPR), quotient filters (cache-friendly), ribbon filters (minimal space). Used in databases (LSM-tree compaction), networks (content routing), and distributed systems.' },
    codeExample: {"language":"python","code":"import hashlib\nclass BloomFilter:\n    def __init__(self, size=100):\n        self.bit_array = [False] * size\n        self.size = size\n    def _hashes(self, item):\n        h1 = int(hashlib.md5(item.encode()).hexdigest(), 16)\n        h2 = int(hashlib.sha1(item.encode()).hexdigest(), 16)\n        return [h1 % self.size, h2 % self.size, (h1 + h2) % self.size]\n    def add(self, item):\n        for i in self._hashes(item): self.bit_array[i] = True\n    def check(self, item):\n        return all(self.bit_array[i] for i in self._hashes(item))","description":"Simple Bloom Filter"},
    relatedTerms: ['hash-function', 'hash-table', 'probabilistic-data-structure'],
    historicalContext: 'Invented by Burton Howard Bloom in 1970.',
    tags: ['probabilistic', 'set', 'space-efficient'] }
];
